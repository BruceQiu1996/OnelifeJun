@page "/"
@inject NavigationManager NavigationManager;
@code {

    protected async override Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            NavigationManager.NavigateTo("/records", false, true);
        }
    }
}
